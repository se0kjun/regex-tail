#regex-tail

##install

	git clone https://github.com/se0kjun/regex-tail.git
	cd ./regex-tail
	npm install -g

##developer

	git clone https://github.com/se0kjun/regex-tail.git
	cd ./regex-tail
	npm install
	npm link

